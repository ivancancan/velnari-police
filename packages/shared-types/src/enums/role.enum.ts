export enum UserRole {
  ADMIN = 'admin',
  OPERATOR = 'operator',      // Operador de cabina
  SUPERVISOR = 'supervisor',  // Supervisor de turno
  COMMANDER = 'commander',    // Comandante / director
  FIELD_UNIT = 'field_unit',  // Policia en campo
}
